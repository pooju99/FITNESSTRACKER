name: fitness_tracker
description: A web-compatible Fitness Tracker App

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '^3.9.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2
  fl_chart: ^0.63.0
  provider: ^6.1.2
  google_fonts: ^6.1.0
  animations: ^2.0.8
  flutter_local_notifications: ^17.1.2
  timezone: ^0.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
